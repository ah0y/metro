<h2>Listing Rooms</h2>

<table class="table">
  <thead>
    <tr>
      <th>Capacity</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for room <- @rooms do %>
    <tr>
      <td><%= room.capacity %></td>

      <td class="text-right">
        <span><%= link "Show", to: Routes.room_path(@conn, :show, room), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: Routes.room_path(@conn, :edit, room), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: Routes.room_path(@conn, :delete, room), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Room", to: Routes.room_path(@conn, :new) %></span>
