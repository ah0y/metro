<h2>Listing Reservations</h2>

<table class="table">
  <thead>
    <tr>
      <th>Expiration date</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for reservation <- @reservations do %>
    <tr>
      <td><%= reservation.expiration_date %></td>

      <td class="text-right">
        <span><%= link "Show", to: reservation_path(@conn, :show, reservation), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: reservation_path(@conn, :edit, reservation), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: reservation_path(@conn, :delete, reservation), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Reservation", to: reservation_path(@conn, :new) %></span>
